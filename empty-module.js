// Intentionally empty. Used by next.config.js to replace Next's legacy
// `polyfill-module` (Array.prototype.at, Object.hasOwn, String.prototype.trimStart,
// URL.canParse, etc.), which our modern browserslist makes unnecessary.
